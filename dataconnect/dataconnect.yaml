specVersion: "v1beta"
serviceId: "inventory-system"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "inventory-system-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
